{
  "name": "client",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "lint": "tsc --noEmit && eslint . --cache --fix --ext .ts,.tsx"
  },
  "devDependencies": {
    "eslint": "^6.6.0"
  }
}
